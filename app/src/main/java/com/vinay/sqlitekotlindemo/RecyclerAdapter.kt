/*
package com.vinay.sqlitekotlindemo

import android.content.Context
import android.view.View
import android.widget.ImageView
import android.widget.TextView
import androidx.recyclerview.widget.RecyclerView

class RecyclerAdapter {

    inner class HolderRecord(itemView: View) : RecyclerView.ViewHolder(itemView) {

        private var context: Context? = null
        private var recordList: ArrayList<ModelRecords>? = null

        constructor(context: Context?, recordList: ArrayList<ModelRecords>?) : super() {

            this.context = context
            this.recordList = recordList

        }

        // view from row_records
        var profileIv: ImageView = itemView.findViewById(R.id.profileIv)
        var tvName: TextView = itemView.findViewById(R.id.tvName)
        var tvPhone: ImageView = itemView.findViewById(R.id.tvPhone)
        var tvEmail: ImageView = itemView.findViewById(R.id.tvEmail)
        var tvDob: ImageView = itemView.findViewById(R.id.tvDob)

    }


}*/
